[project]
name = "rename-files"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "magic-python>=0.4.0",
    "psutil>=7.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-mock>=3.15.1",
]
